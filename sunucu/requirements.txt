fastapi==0.115.6
uvicorn[standard]==0.34.0
# Goruntu isleme (goruntu.py, tespit.py) icin. Ikisi de ISTEGE BAGLI:
# main.py tembel ice aktariyor ve bulamazsa yalniz gorüntü uc noktalari
# kapaniyor — sulama, ekim ve hareket etkilenmiyor. Eski bir kurulumda
# bunlar yoksa sunucu yine aciliyor.
numpy>=1.24
Pillow>=10.0

# AprilTag kalibrasyonu (etiket.py) icin. ISTEGE BAGLI: kurulu degilse
# yalniz etiket yolu kapaniyor, elle kalibrasyon ve makinenin geri kalani
# calisiyor. Pi'de apt ile gelen surum tercih edilmeli:
#   sudo apt install -y python3-opencv
# Bu satir pip ile kuranlar icin; apt surumu varsa gerekmiyor.
opencv-python-headless>=4.7 ; platform_machine != "aarch64"
